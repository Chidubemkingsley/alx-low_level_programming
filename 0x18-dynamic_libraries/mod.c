#include <stdlib.h>
#include <stdio.h>

/**
 * mod - mod of the integer
 * @a - first integer
 * @b - second integer
 * Return: the final result of the mod
 */

int mod(int a, int b)
{
	return (a % b);
}

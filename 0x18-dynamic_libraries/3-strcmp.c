#include "main.h"
#include<string.h>

/**
 * _strcmp -  function that compares two strings of the integer
 * @s1:string1 to compare of the integer
 * @s2: string two to compare of the integer
 * Return: 0 of the file program
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}

#include "main.h"

/**
 * _abs - responsible to computes the absolute value of an integer
 * @n: n is an integer of the function
 *
 * Return: integer of the function
 */

int _abs(int n)
{
	if (n > 0)
	{
	return (n);
	}
	else if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
}

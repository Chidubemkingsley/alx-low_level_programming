#include <stdlib.h>
#include <stdio.h>

/**
 * mul  - that multiply the integer
 * @a - first integer
 * @b - second integer
 * Return: multiplication of the integer
 */

int mul(int a, int b)
{
	return (a * b);
}
